ext.gradleWrapperVersion = '2.1'
ext.encoding = 'UTF-8'

task wrapper(type: Wrapper) {
	gradleVersion = gradleWrapperVersion
}

tasks.withType(SourceTask) each { it.options.encoding = encoding }
